# 二刷 · 开发环境启动指南

> 更新：2026-09-10 ｜ 适用：Windows + 项目内便携工具链（无需系统安装 Node/Git）

---

## 方式一：双击启动（最简单）

双击项目根目录的 **`启动开发.cmd`**

- 会弹出一个黑色终端窗口，自动启动前端和后端
- **窗口不能关**（关了服务就停了）
- 启动成功后，浏览器打开 **http://localhost:5173**
- 看到案卷页（速读摘要 / 三人物 / 起手证据）即为正常

> 首次启动约需 10-30 秒；终端里会打印 `[vite]` 和 `[server]` 两行就绪信息。

## 方式二：PowerShell 手动启动（双击失败时用）

如果双击没反应（部分系统策略会拦截 .cmd），用这个：

1. 按 `Win` 键 → 输入 `powershell` → 回车
2. 整段复制粘贴进窗口，回车：

```powershell
$env:Path = "d:\知乎黑客松\tools\node-v20.18.0-win-x64;d:\知乎黑客松\tools\mingit\cmd;" + $env:Path
cd d:\知乎黑客松\game
npm run dev
```

3. 浏览器打开 http://localhost:5173

## 方式三：只跑后端（跑 eval / 调试 API 时用）

```powershell
$env:Path = "d:\知乎黑客松\tools\node-v20.18.0-win-x64;" + $env:Path
cd d:\知乎黑客松\game
node server/index.js
# 后端跑在 http://localhost:3001，健康检查：浏览器开 http://localhost:3001/api/health
```

---

## 验证启动成功

| 检查 | 地址 | 期望 |
|---|---|---|
| 前端 | http://localhost:5173 | 案卷页正常渲染 |
| 后端健康 | http://localhost:3001/api/health | `{"ok":true,"llm":true}` |
| LLM 状态 | 同上 `llm` 字段 | `true`=DeepSeek 正常；`false`=走离线剧本（顶栏会有"离线剧本模式"角标） |

## 常见问题

**Q：终端一闪而过 / 双击没反应**
→ 用方式二。

**Q：页面报"案卷调阅失败"**
→ 后端没起来或端口被占。看终端有没有报错；跑 `http://localhost:3001/api/health` 确认。

**Q：`llm:false`（离线剧本模式）**
→ `game/.env` 里的 `DEEPSEEK_API_KEY` 丢失或失效。检查文件是否存在且为 `sk-` 开头；改完重启。

**Q：端口被占用（3001 / 5173）**
→ 结束残留的 node 进程后重启：
```powershell
Get-Process node -ErrorAction SilentlyContinue | Stop-Process -Force
```

**Q：改了 `数据资产/` 下的 JSON，游戏里没生效**
→ 需要重跑迁移再重启：
```powershell
$env:Path = "d:\知乎黑客松\tools\node-v20.18.0-win-x64;" + $env:Path
cd d:\知乎黑客松\game
node scripts/migrate_data.js
```

## 相关文档

- 产品/流程：`故事侦探PRD.docx`（历史名）｜ `开发Spec.md`（现行，v1.2）
- 设计规范：`design.md`
- 各阶段验收报告：`game/eval/reports/`
