# 二刷 · Design.md 设计规范

> 版本 v1.1 ｜ 2026-09-07 ｜ 基准原型：`原型/原型B_案件卷宗.html`
> v1.1：新增 2.1 硬性禁令（AI 味渐变背景、卡片套卡片），同步更新第 9、12 章。
> 本文档\*\*取代 PRD 第五章（视觉与交互）\*\*中 Noir 黑色电影方向的全部内容，是后续所有页面开发的唯一视觉依据。任何页面产出后须对照本文档第 12 章自检。

***

## 1. 设计概念：案件卷宗（Paper Dossier）

**一句话**：整个游戏是"一宗被人翻开的旧案卷"——玩家不是在操作软件，是在翻阅、批注、拼贴一份泛黄的档案。

**为什么是它**：

- 叙事贴合：游戏内容本身就是"案卷移交—询问笔录—证据拼板—对质—结案"，视觉与玩法同构；

- 人群贴合：盐言读者（女性为主、情感消费型）对"信纸、老照片、手写批注"的亲和度远高于硬核 Noir 审讯室的冷峻压迫；

- 工程贴合：纸质纹理、印章、毛线等元素用 CSS/SVG 即可实现，且天然掩盖 AI 生图的细节瑕疵。

**核心理念：纸是有年龄的。** 一切组件都应像被人的手翻过：有折痕、有茶渍、有图钉孔、有手写批注。禁止出现"崭新的、屏幕感的、发光的"元素。

***

## 2. 设计原则（新增组件时的判断标准）

1. **纸优先于屏**：任何界面元素首先问"它能不能是一张纸/一个纸上的物件？"——能，就做成纸；不能（如输入框），也必须"落在纸上"（纸面 + 墨线边框）。
2. **暖旧色，不用纯黑白**：主色调是"放了十年的档案纸"的暖黄，不是白纸。对比靠墨色深浅，不靠高饱和撞色。
3. **手作痕迹 > 数字精确**：卡片轻微旋转（-2° \~ +2°）、图钉位置不完全居中、红线略松垮——克制的不完美感是风格本体。
4. **红色只给"罪证与警示"**：印泥红是唯一强调色，语义固定为"矛盾/警告/关键解锁"。禁止用它做装饰。
5. **动效即"翻案动作"**：所有动效必须能翻译成一个物理动作——钉上、盖下、抽出、划线。禁止无物理对应的花哨动画。

### 2.1 硬性禁令（绝对禁止，无例外，优先级高于一切组件规范）

1. **禁止使用 AI 味浓的渐变色背景。** 具体指：多色相渐变（紫→蓝→粉、青→紫等）、mesh 渐变、极光/流光效果、大面积高饱和渐变铺底——即典型的"AI 生成页面"审美。

   - **判定标准**：渐变若用于"展示色彩本身"（装饰性），禁止；渐变若用于"模拟光影与材质"（单色系、低饱和、小范围），允许。

   - 允许的渐变白名单（仅限以下）：`--desk` 桌面单色系径向渐变（灯光落在桌上的 vignette）、茶渍/做旧的 `radial-gradient`（单色透明度衰减）、`.dialog` 横线纸的 `repeating-linear-gradient`（纸上线条，非色彩渐变）、照片做旧的单色 fade。新增渐变必须能归入"光影/材质"类，否则不加。
2. **禁止卡片套卡片。** 卡片（`.ecard` 家族：`.note` / `.polaroid` / `.index`）不得互相嵌套；卡片内部也不得再出现"带边框 + 底色 + 阴影"的卡片式容器。

   - 层级上限：`舞台(.stage) → 面板(.board / 抽屉 / 笔录区) → 卡片(.ecard)`，**到卡片为止**。

   - 容器区分：`.board`（牛皮板面）、抽屉、`.stage` 是"面板/桌面"，不是卡片，可以承载卡片；但面板内不得再套一层"面板样式的面板"。

   - 卡片内需要分组信息时，用墨线分隔（`border-top: 1px solid var(--paper-line)`）或段距，不引入新的框。

***

## 3. 色彩系统（Design Tokens）

### 3.1 CSS 变量定义（全局唯一来源，所有页面必须引用）

```css
:root {
  /* 底色 */
  --desk:        #241f18;  /* 桌面（深棕，仅页面外围背景） */
  --paper-bg:    #e9dfc6;  /* 卷宗纸底（主背景） */
  --paper-hi:    #f4ecd8;  /* 高光纸（输入框、卡片纸面提亮） */
  --paper-line:  #c9b992;  /* 纸上墨线（边框、分隔线、横线） */

  /* 墨色（文字） */
  --ink:         #3a332a;  /* 正文墨 */
  --ink-soft:    #5a5142;  /* 次级墨 */
  --ink-faint:   #8a7450;  /* 淡墨（占位、辅助说明） */

  /* 印泥红（唯一强调色） */
  --stamp:       #a03024;  /* 印章红（矛盾、警示、主按钮） */
  --stamp-soft:  #c96b5d;  /* 浅印红（停顿提示、批注） */

  /* 牛皮纸（证据板） */
  --kraft:       #d8c49a;  /* 牛皮纸板面 */
  --kraft-line:  #b9a887;  /* 牛皮纸边框 */
}
```

### 3.2 用法规则

| 色彩           | 允许的用途                                             | 禁止的用途             |
| ------------ | ------------------------------------------------- | ----------------- |
| `--desk`     | 仅 body 背景（radial-gradient 中心 #2e2820 → 边缘 --desk） | 任何组件内部            |
| `--paper-bg` | 主舞台、笔录区、卷宗页                                       | —                 |
| `--paper-hi` | 输入框、索引卡、可交互纸片                                     | 大面积铺色             |
| `--ink` 三档   | 按"正文 → 次级 → 占位"层级递减                               | 用纯黑 #000          |
| `--stamp`    | 矛盾章、"未结"章、主按钮、钉子、红毛线、动摇标记                         | 装饰、大面积填充、超过 3 处同屏 |
| `--kraft`    | 仅证据板板面                                            | 其他区域              |

**对比度底线**：正文 `--ink` on `--paper-bg`；任何文字不得低于 `--ink-faint` on `--paper-hi` 的对比强度。

***

## 4. 字体系统

### 4.1 字族

```css
/* 正文字族：仿宋 */
--font-body: FangSong, 'STFangsong', 'FangSong_GB2312', serif;
/* 标题/印章/批注字族：楷体 */
--font-title: 'KaiTi', 'STKaiti', 'KaiTi_GB2312', serif;
/* 编号/时间戳等机打字族 */
--font-mono: 'Courier New', Courier, monospace;
```

- 公文/笔录正文 = 仿宋（"询问笔录"的既视感）；

- 人名、印章、卡片标题、批注、按钮 = 楷体（手写公文感）；

- 档案编号、日期、页码 = Courier（打字机/钢印数字感）。

- **禁止**使用黑体、微软雅黑等无衬线字体出现在舞台内（仅允许舞台外的开发者说明文字）。

### 4.2 字号与字距（舞台 980×560 基准）

| 场景            | 字号        | 字距      | 字体      | 颜色                  |
| ------------- | --------- | ------- | ------- | ------------------- |
| 卷宗标题          | 16px      | 5px     | 楷体 bold | #4a4132             |
| 章节标签（如"询问笔录"） | 12px      | 4px     | 仿宋      | --ink-faint         |
| 笔录正文（问/答）     | 13.5px    | 0       | 仿宋      | --ink               |
| 卡片正文          | 12px      | 0       | 仿宋      | --ink               |
| 卡片标签（tag）     | 9.5\~10px | 3px     | 楷体      | #8a7450             |
| 档案编号          | 11px      | 1px     | Courier | #7a6f5a             |
| 按钮            | 11.5px    | 3px     | 仿宋      | 见组件规范               |
| 印章文字          | 14\~15px  | 3px（竖排） | 楷体 bold | rgba(160,48,36,.62) |

行高：笔录正文 1.8；卡片 1.6；单行组件 1。

***

## 5. 质感与氛围（全局氛围层，每页必备）

### 5.1 纸张纹理（SVG 滤镜，全局复用）

每个舞台页面引入一次（`<svg>` 隐藏 + filter 引用）：

```html
<svg width="0" height="0" style="position:absolute">
  <filter id="paperF">
    <feTurbulence type="fractalNoise" baseFrequency="0.045 0.06" numOctaves="5" seed="7" result="n"/>
    <feDiffuseLighting in="n" lighting-color="#ffe9c8" surfaceScale="1.6">
      <feDistantLight azimuth="235" elevation="55"/>
    </feDiffuseLighting>
  </filter>
</svg>
```

使用：`.tex { position:absolute; inset:0; filter:url(#paperF); opacity:.55; mix-blend-mode:multiply; pointer-events:none; z-index:50; }`

规则：
- `seed` 值每个页面固定不同（避免两张纸纹理完全一样），但 `baseFrequency` 与 `opacity` 不变；
- 纹理层永远在最顶（z-index 50），`pointer-events:none`，绝不遮挡交互；
- **实现红线（2026-09-10 实证）：纸纹只能用 `.tex` 覆盖层实现，禁止对任何内容容器直接加 `filter:url(#paperF)`**——feDiffuseLighting 会把容器内全部内容（含文字）替换为光照纹理输出。曾导致原文抽屉"只剩纸底无文字"。

### 5.2 茶渍 / 咖啡渍（氛围点缀）

```css
.stain { position:absolute; border-radius:50%; pointer-events:none; z-index:1;
  background:radial-gradient(circle, rgba(140,100,40,.12), transparent 70%); }
```

- 每页最多 2 处，尺寸 80\~140px，放置在**不遮挡文字**的角落；

- 深浅随机（透明度 .08\~.14），模拟旧纸自然痕迹，不做完美圆形。

### 5.3 做旧三件套（按需）

- **折痕**：`linear-gradient` 对角线亮/暗 1px 双线，opacity .15；

- **毛边**：卡片纸边用 `box-shadow: inset 0 0 6px rgba(120,90,30,.15)`；

- **烧焦/水渍边**：仅结算页高潮时刻可用，普通页面禁用。

***

## 6. 布局系统

### 6.1 舞台框架（所有页面统一）

```
body（--desk 径向渐变背景，居中）
└── .stage  980 × 640，--paper-bg，overflow:hidden（高度经 demo 验证由 560 修订为 640，容纳审讯界面信息密度）
    ├── .topbar  顶栏（约 48px 高，底部 2px --paper-line 边框）
    └── .main    主区（flex，剩余高度）
```

- 舞台外：开发者注释文字（12px，#8a7c5f，字距 4px，居中于舞台上方）；

- 舞台内边距：topbar 左右 18px；主区四边 16\~18px；

- **主区两栏比例固定**：左栏（审讯/内容区）flex 1.22，右栏（证据板）flex 1，间隙 8px。除结算页、案卷页可变外，审讯室页面必须保持此比例。

### 6.2 响应式策略

MVP 为桌面端演示（1280×800 截图/投屏基准）。最小适配宽度 1024px；不要求移动端。舞台居中缩放：`@media (max-width:1024px) { .stage { transform: scale(...) } }` 即可。

***

## 7. 组件规范（核心库）

> 以下组件均已在本基准原型中实现，开发时直接提取 class 复用，不重写样式。

### 7.1 顶栏（.topbar）

```
左：卷宗标题（楷体 bold 16px + 章节标签）
右：档案编号（Courier，格式 YS-MMDD，MMDD=案发日期 0914）
    暗线计数章（.chip：--stamp 边框 + 文字，rotate(-1.5deg)）
    案卷原文按钮（.chip.file：纸片样式，hover 变 --paper-hi）
```

变体：不同阶段右侧只保留该阶段相关元素；"暗线 0 / 3"的数字用 `<b>` 加粗 --stamp。

### 7.2 「未结」印章（.seal）

- 双圈红章：外 2.5px 边框 + 内 1px 虚线圈，`rgba(160,48,36,.55/.4)`，整体 `rotate(12deg)`；

- 竖排"未结"楷体字，颜色 `rgba(160,48,36,.62)`；

- 位置：顶栏右下、不遮内容；**半透明叠印在纸面上**（模拟盖章后渗透），不做实心红块；

- 状态变化：全部暗线解锁后，此章变为"已结"（同款式，内容换字，可加轻微盖章动画）。

### 7.3 角色证件照（.photo）

替代传统立绘的方案——**每个角色一张"档案证件照"**：

```
结构：白边相纸（padding 7px，rotate(-2°)，顶部红图钉）
     └─ 照片区（112×112）：AI 生成的棕褐色老照片
     └─ 姓名条（楷体，13px，字距 5px）
状态行（.pstate）：仿宋 11px，字距 3px
```

- 三个角色照片旋转角度分别为 -2° / +1.5° / -1°（各自固定，制造"随手钉上"感）；

- **状态三档的视觉表现**：

  - 平静：照片原样；

  - 动摇：照片区叠加 `rgba(160,48,36,.06)` 红晕 + 轻微 `contrast(.94)`；

  - 失守：照片整体 `grayscale(.6) brightness(.92)` + 相纸边角加深折痕。

- 做旧处理在生图后统一进行（见第 11 章）。

### 7.4 询问笔录对话流（.dialog）

**格式**：`问：`/`答：` 前缀（问=--stamp，答=--ink，bold），正文仿宋 13.5px / 行高 1.8。

**横线纸背景**（必加，笔录感的核心）：

```css
.dialog {
  background: repeating-linear-gradient(
    transparent 0 28px, rgba(120,95,50,.18) 28px 29px);
}
```

**元素规格**：

- 玩家提问 = `问：`行，右对齐可选（MVP 采用左对齐笔录体，更贴公文）；

- 停顿标记 = `（沉默片刻）`，--stamp-soft；

- 动摇标记 = `.shaken-mark`：--stamp 边框小标签，`rotate(-3deg)`，内容"动 摇"（字距撑开）；

- 系统事件行 = `〔 出示物证 · 朋友圈的松鼠鳜鱼 〕`：上下 1px 虚线 --paper-line，--stamp 文字；

- 钉住按钮 = `.pin-btn`：10px，--stamp 边框，hover 淡红底。

### 7.5 输入区（.inputrow）

```
输入框：--paper-hi 底 + 1px --paper-line 边框，无圆角（0px，公文纸片感），
        占位文字 --ink-faint 仿宋 12.5px
按钮组（.btnrow）：
  主按钮 .btn.primary：--stamp 底 #f4e8dc 字，边框 #7e2419，hover #8e2a1f
  次按钮 .btn.ghost：--paper-hi 底 + --paper-line 边框 + --ink-soft 字
```

- 全站按钮**无圆角、无阴影**（纸片不是塑料）；

- 「需要思路？」按钮文案固定带问号，避免歧义（点击才展开，见 PRD 3.4）。

### 7.6 证据板（.board）

```
板面：--kraft 底 + 1px --kraft-line 边框 + inset 阴影 rgba(120,90,30,.28)
点阵纹理：radial-gradient(#231a10 1px 透明) 6×6px，opacity .4
标签："证 据 板" 楷体 12px 字距 7px #6a5636
```

**红毛线（.thread）**：SVG `path`，`stroke:#a03123`，`stroke-width:1.8`（vector-effect:non-scaling-stroke），曲线 `C` 手绘感（非直线），两端点对准卡片图钉位置。

### 7.7 证据卡三类（.ecard 家族）

| 类型  | class       | 纸面                         | 适用                      |
| --- | ----------- | -------------------------- | ----------------------- |
| 便签纸 | `.note`     | #f2e9c9                    | 口供卡（角色台词摘录）             |
| 拍立得 | `.polaroid` | #fdfaf2 + 白边 + 底部楷体图说      | 物证卡（朋友圈截图、收据等，配 SVG/图片） |
| 索引卡 | `.index`    | --paper-hi + 横线纹理（21px 间隔） | 结构性信息卡（钥匙清单、时间线条目）      |

通用规则：

- 三类卡片均带**顶部红图钉**（9px 圆点 --stamp + 阴影），位置 `left:50%` 微偏移；

- 旋转 -2° \~ +2° 各自固定；

- 卡片标签（tag）格式：`类型 · 来源`（如"口供 · 陈默"/"物证 · 原文（一）"）；

- 阴影统一 `1px 3px 9px rgba(80,60,20,.3)`（比 Noir 版更轻，纸片贴板的感觉）。

### 7.8 矛盾标记（.clash）

- 空心红框章：2.5px --stamp 边框 + 半透明纸底 `rgba(244,236,216,.6)`，`rotate(-6deg)`，楷体 12px 字距 4px；

- 内容固定："⚡ 矛盾"（印证时用"✓ 印证"，同款式）；

- 位置：跨在两张卡之间的红毛线中点上方。

### 7.9 案卷原文抽屉（其他页面复用）

- 抽屉本体：从右侧滑入的 380px 宽纸页（--paper-bg + 纸纹 + 左侧 2px --paper-line）；

- 原文排版：仿宋 13px / 行高 2.0，段首不缩进、段落间 12px，章节号"（一）"楷体；

- **荧光笔划选**：`::selection { background: rgba(201,107,93,.4); }`（浅印红荧光感）；

- 「以此为证」按钮：选中文字后浮出，.btn.primary 同款。

***

## 8. 动效规范（全部 = 物理动作）

| 交互   | 动效                                                                                        | 时长/曲线               |
| ---- | ----------------------------------------------------------------------------------------- | ------------------- |
| 钉住口供 | 卡片从台词处"飞"到证据板 + 图钉钉下（scale 1.1→1 + 轻微 rotate 归位）+ "嗒"声                                    | 280ms ease-out      |
| 矛盾连线 | 红线从 A 图钉"拉"到 B 图钉（stroke-dashoffset 动画）+ 矛盾章"盖下"（scale 1.6→1 + opacity 0→1，rotate 保持 -6°） | 400ms / 220ms 两段    |
| 出示证据 | 卡片从证据板"抽出"插入对话流（translateY + fade）                                                        | 240ms               |
| 角色动摇 | 照片红晕渐显 + 台词打字机逐字（每字 24ms）                                                                 | —                   |
| 对峙成功 | 全屏纸面瞬间压暗（rgba(36,31,24,.25) overlay 120ms）后恢复 + 角色照片切"失守"态                                | 分两拍                 |
| 暗线解锁 | 原文抽屉自动展开，对应句子逐条红框标注 + "钉入"证据板                                                             | 600ms stagger 120ms |
| 结算   | 档案纸从底部打印机逐行"吐出"（clip-path inset 动画 + 打印机音效）                                               | 逐行 80ms             |

**总原则**：单动效 ≤ 400ms；连续反馈（打字机）除外。全部动效必须尊重 `prefers-reduced-motion`。

***

## 9. 状态与反馈语义

| 语义       | 视觉载体                                    | 唯一性          |
| -------- | --------------------------------------- | ------------ |
| 矛盾/警示/关键 | --stamp 红（章、线、钉、标签）                     | 全屏红色元素 ≤ 3 处 |
| 印证/进展    | --stamp-soft 浅红 或 --ink-soft 墨          | 不引入新色相       |
| 无关/失败    | 不惩罚：淡墨化（opacity .6）+ "过度解读"收藏标          | 不用灰绿色系       |
| 可交互      | 边框加深 + 纸面提亮（--paper-hi）+ cursor:pointer | 不用蓝色链接色      |
| 角色防线     | 不做数值条（推翻原型 C 方案）——用照片状态三档 + 台词细节表达      | —            |

**明确弃用清单**（来自未选中的原型 A/C，禁止混入）：胶片颗粒滤镜、暗角、台灯光晕、扫描线、霓虹发光、心理防线进度条、气泡圆角对话框。

***

## 10. 全页面清单与组件套用

| 阶段（PRD 3.2） | 页面            | 布局               | 复用组件                 | 特有组件                                            |
| ----------- | ------------- | ---------------- | -------------------- | ----------------------------------------------- |
| 0 情绪转场      | 故事结尾页         | 竖排故事末段 + 底部入口    | 纸纹、茶渍                | 入口按钮做成"另附案卷"纸条（rotate 1°）："故事里还有 3 个人没说实话 →"    |
| 1 案卷移交      | 案卷封面页         | 居中纸页             | topbar、seal（"未结"）、纸纹 | ①速读摘要（索引卡叠放）②人物关系图（手绘风 SVG，--ink 线条）③时间线（索引卡横排） |
| 2-4 审讯室     | 主界面（基准原型）     | 两栏 1.22 : 1      | 全部（见第 7 章）           | —                                               |
| 2-4 附属      | 案卷原文抽屉        | 右侧滑入 380px       | 纸页、荧光笔               | 「以此为证」                                          |
| 4 对峙时刻      | 全屏演出          | 纸面压暗 overlay     | 台词逐行显现（仿宋加大 15px）    | 证据链逐张飞入 + 最终红章"对质"盖下                            |
| 5 真相结算      | 结案页           | 打印机吐纸（竖向滚动）      | 印章（"已结"）、索引卡         | 暗线对照表（你的推理 vs 官方暗线）、侦探档案、原文坐标回放链接               |
| 6 分享卡       | 结算卡（MVP mock） | 独立小纸片（如 420×300） | 纸纹、印章、楷体             | "破案"大章 + 关键数据（6/9 伏笔等）                          |

***

## 11. 图像生成规范（角色照片与物证图）

所有 AI 生图（角色证件照 3 张 + 物证图若干）统一基调，**生图后必须过一遍做旧处理**：

- **Prompt 基调模板**：
  `1990s Chinese ID photo style portrait, sepia tone, film grain, slightly faded edges, plain light background, [角色描述], solemn expression, front-facing, vignette`

  - 陈默：40 岁上下男性，衬衫，克制疲惫；周兰：60 多岁女性，整洁灰发；小雅：27 岁女性，黑眼圈，倦容。

- **统一后处理**（CSS 即可，不依赖 PS）：
  `filter: sepia(.35) contrast(.92) brightness(.96);`
  相纸上再叠纸纹滤镜（同 5.1）。

- **物证图**（朋友圈截图、收据、客栈照片）：以"翻拍的老照片"呈现——加白边（拍立得）、轻旋转、底部楷体图说；

- **一致性红线**：三张证件照必须同批次生成（同一模型、同一 prompt 结构、同参数），确保色调统一；不得混用不同风格来源的图。

***

## 12. 页面自检清单（每个页面产出后过一遍）

- [ ] 色彩：只用了第 3 章 token？全屏 --stamp 红元素 ≤ 3 处？无纯黑/纯白/蓝紫色？

- [ ] 字体：舞台内只有仿宋/楷体/Courier？字号字距符合 4.2 表？

- [ ] 质感：纸纹滤镜在最顶层且 pointer-events:none？茶渍 ≤ 2 处且不压文字？

- [ ] 组件：复用了第 7 章 class 而非重写？卡片有图钉、有微旋转？按钮无圆角无阴影？

- [ ] 动效：每个动画都能说出对应的物理动作？单段 ≤ 400ms？

- [ ] 硬性禁令：无多色相/mesh/极光渐变背景（渐变只用于白名单内的光影材质）？无卡片嵌套卡片（层级止于 .stage → 面板 → 卡片）？

- [ ] 弃用清单：无胶片颗粒/暗角/扫描线/发光/进度条/圆角气泡混入？

- [ ] 交互态：hover 有纸面提亮反馈？::selection 是浅印红荧光？

***

## 13. 技术落地约定

1. **Token 单源**：`design-tokens.css` 全局引入，禁止页面内硬编码色值（原型演示页除外，因其需独立打开）；
2. **纸纹滤镜**：`paper-texture.svg` 作为公共片段 include，每页换 seed；
3. **组件库文件**：`components.css` 承载第 7 章全部 class，命名与本文档一致（`.ecard/.note/.polaroid/.index/.clash/.seal/.chip` 等）；
4. **字体加载**：仿宋/楷体依赖系统字体（演示环境为 Windows，安全）；`Courier New` 同理；不引外部字体，避免线上 demo 加载风险；
5. **立绘占位**：开发期用基准原型中的 SVG 剪影占位，D6 替换 AI 生图后立即按第 11 章做旧。

***

*本文档由原型 B 提取固化。若后续需要调整风格，先改本文档，再改代码——保证"规范先行"。*
